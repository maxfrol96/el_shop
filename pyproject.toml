[tool.poetry]
name = "el-shop"
version = "0.1.0"
description = ""
authors = ["maksfrol96 <maxfrol96@mail.ru>"]
readme = "README.md"
packages = [{include = "el_shop"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
